import random
def game(): 
    Li1 = ["rock","paper","scissor"]
    computer=random.choice(Li1)
    user=input("enter ur choice: ")
    print("system's choice:",computer)
   
    if (computer == user):
        print("TIE") 
        return "t"
    elif computer =="rock":
        if user=="scissor":
            print("Computer wins") 
            
            return "c"
        else :
            print("User wins") 
            
            return "d"
    elif computer=="paper":
        if user=="rock":
            print("Computer wins") 
            return "c"
        else:
            print("User wins") 
            
            return "d"
    elif computer=="scissor":
        if user=="paper":
            print("Computer wins") 
            
            return "c"
        else:
           print("User wins") 
           
           return "d"


n=int(input("Number of games you want to play ? : "))
c=0
d=0
t=0

for i in range(n):
    e=game()
    if e=="c":
     c=c+1
    elif e=="d":
     d=d+1
    elif e=="t":
     t=t+1

print("computer score : ",c)
print("user score :",d)
print("no. of tie : ",t)
if(c>d):
    print("COMPUTER WINS")
elif(c<d):
    print("USER WINS")
else:
    print("TIE ")





  






O/P

Number of games you want to play ? : 3
enter ur choice: rock
system's choice: scissor
User wins
enter ur choice: rock   
system's choice: scissor
User wins
enter ur choice: rock   
system's choice: paper
Computer wins
computer score :  1   
user score : 2        
no. of tie :  0       
USER WINS
  

